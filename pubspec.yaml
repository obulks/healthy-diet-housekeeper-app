name: healthy_diet_housekeeper

description: Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  json_model: #latest version
  dio: ^3.0.10
  shared_preferences: ^0.5.12+4
  provider: ^4.3.2+2
  device_info: ^1.0.0
  percent_indicator: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
    - assets/icons/food_group/

